# ── LangChain Ecosystem ──────────────────────────────────────────────
langchain-core>=0.3.0
langchain-anthropic>=0.3.0
langchain-community>=0.3.0
langchain-chroma>=0.1.0
langgraph>=0.2.0
langsmith>=0.2.0

# ── LLM Caching ──────────────────────────────────────────────────────
langchain[sqlite]>=0.3.0

# ── Embeddings ───────────────────────────────────────────────────────
# chromadb bundles its own ONNX embedding (no Rust required)
# fastembed removed — requires Rust/MSVC to compile on Windows
# Optional: pip install fastembed  (if you have Visual C++ Build Tools)

# ── HTTP & Web Intelligence ───────────────────────────────────────────
httpx>=0.27.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
extruct>=0.16.0
newspaper3k>=0.2.8
ddgs>=0.1.0

# ── NLP & Content Quality ─────────────────────────────────────────────
# keybert is optional — falls back to word-frequency extraction if unavailable
# Optional: pip install keybert  (requires torch, may fail on Python 3.14)
textstat>=0.7.0

# ── Memory ────────────────────────────────────────────────────────────
chromadb>=0.5.0
sqlalchemy[asyncio]>=2.0.0
aiosqlite>=0.20.0
alembic>=1.13.0

# ── Async File I/O ────────────────────────────────────────────────────
aiofiles>=23.2.0

# ── Config ────────────────────────────────────────────────────────────
pydantic>=2.7.0
pydantic-settings>=2.3.0

# ── Scheduling ────────────────────────────────────────────────────────
APScheduler>=3.10.0

# ── Data ──────────────────────────────────────────────────────────────
pandas>=2.2.0
jinja2>=3.1.0

# ── CLI & Terminal ────────────────────────────────────────────────────
typer>=0.12.0
rich>=13.0.0
loguru>=0.7.0

# ── Resilience ────────────────────────────────────────────────────────
tenacity>=8.2.0
anyio>=4.0.0

# ── Testing ───────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
freezegun>=1.5.0

# ── Stretch: Webhook endpoint ─────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
